from django.apps import AppConfig


class AppAcpConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "app_acp"
    verbose_name = "ACP (Agent Communication Protocol)"
