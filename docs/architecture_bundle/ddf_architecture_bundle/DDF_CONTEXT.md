# DDF CONTEXT

O DDF (Detect & Delegate Framework) é um orbital de orquestração dentro do Core_SinapUm.

Seu objetivo é detectar intenções e delegar execução a provedores (prompt, openmind_http, pipeline, ddf).

Integra com mcp_service na porta 7010 e expõe providers configuráveis.
