from django.apps import AppConfig


class AppSinaplintConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "app_sinaplint"
    label = "app_sinaplint"
    verbose_name = "SinapLint (motor de lint arquitetural + SaaS opcional)"
