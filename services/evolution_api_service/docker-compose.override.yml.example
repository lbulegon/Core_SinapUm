# Modo BUILD (patch 21/jan - PR #2365)
# Copie para docker-compose.override.yml para ativar:
#   cp docker-compose.override.yml.example docker-compose.override.yml
# Ou use: ./scripts/up-build.sh (que usa -f override automaticamente)

services:
  evolution-api:
    image: evolution-api:build-qrfix
    build:
      context: .
      dockerfile: Dockerfile.evolution
