# Evolution API Service
# 
# NOTA: Os volumes de dados (pg_data, redis_data, instances, storage) 
# podem permanecer em /root/evolution_api/ se já estiverem em uso.
# 
# Para mover os volumes, atualize os caminhos no docker-compose.yml:
# - ./pg_data -> /root/evolution_api/pg_data (ou novo caminho)
# - ./redis_data -> /root/evolution_api/redis_data (ou novo caminho)
# - ./instances -> /root/evolution_api/instances (ou novo caminho)

