from django.apps import AppConfig


class AppBillingConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "app_billing"
    verbose_name = "Billing Stripe (execução)"
